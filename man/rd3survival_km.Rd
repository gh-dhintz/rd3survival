% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rd3survival_km.R
\name{rd3survival_km}
\alias{rd3survival_km}
\title{Kaplan-Meier survival plot from a data frame (survminer-style API)}
\usage{
rd3survival_km(
  df,
  time_var,
  event_var,
  strat_var = NULL,
  color = NULL,
  title = NULL,
  endpoint = NULL,
  time_unit = NULL,
  x_label = "Time (months)",
  y_label = NULL,
  x_break_every = 4,
  show_median = TRUE,
  conf_int = TRUE,
  y_format = NULL,
  y_tick_count = 5,
  subtitle = NULL,
  title_x = NULL,
  subtitle_x = NULL,
  title_font_size = NULL,
  subtitle_font_size = NULL,
  bg_color = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{df}{A data frame containing at minimum the time and event columns.}

\item{time_var}{Name (string) of the column holding follow-up time.}

\item{event_var}{Name (string) of the column holding the event indicator
(0 = censored, 1 = event).}

\item{strat_var}{Name (string) of the column to stratify by.  When
\code{NULL} (default) a single-arm curve is drawn.}

\item{color}{A single CSS colour string (single-arm) or a character vector
of colours with one element per level of \code{strat_var} (in factor
order).  \code{NULL} uses the D3 category-10 palette.}

\item{title}{Chart title.}

\item{endpoint}{Short endpoint label shown in the annotation table header,
e.g. \code{"OS"}, \code{"PFS"}.}

\item{time_unit}{Time unit appended to the endpoint label in parentheses,
e.g. \code{"Months"}, \code{"Days"}.}

\item{x_label}{X-axis label.  Default \code{"Time (months)"}.}

\item{y_label}{Y-axis label.  Defaults to
\code{paste0("Probability of ", event_var)}.}

\item{x_break_every}{Numeric interval between x-axis tick marks and
number-at-risk columns.  Default \code{4}.}

\item{show_median}{Logical.  When \code{TRUE} (default), draws dashed
median crosshairs and the annotation table.}

\item{conf_int}{Logical.  When \code{TRUE} (default), shades 95\%
confidence-interval bands around each curve.}

\item{y_format}{D3 format string controlling y-axis tick labels:
\describe{
  \item{\code{".2\%"}}{100.00\% (default)}
  \item{\code{".0\%"}}{100\%}
  \item{\code{".2f"}}{1.00}
  \item{\code{".0f"}}{1}
}}

\item{y_tick_count}{Approximate number of y-axis ticks.  Default \code{5}
produces ticks at roughly 0\%, 25\%, 50\%, 75\%, 100\%.  Passed to
D3's \code{axis.ticks(n)}; the actual count may differ slightly.}

\item{subtitle}{Optional subtitle rendered in smaller text below the title.}

\item{title_x}{Horizontal start position of the title as a proportion of
the total SVG width (0 = far left, 1 = far right).  \code{NULL} (default)
centres the title.}

\item{subtitle_x}{Horizontal start position of the subtitle on the same
0–1 scale.  \code{NULL} (default) centres the subtitle.}

\item{title_font_size}{Title font size in pixels.  \code{NULL} uses the
CSS default (16 px).}

\item{subtitle_font_size}{Subtitle font size in pixels.  \code{NULL} uses
the CSS default (12 px).}

\item{bg_color}{SVG background colour as a CSS colour string.  Default
\code{"#232b2b"}.}

\item{width, height}{Widget dimensions passed to
\code{\link[htmlwidgets]{createWidget}}.  \code{NULL} fills the container.}

\item{elementId}{Optional fixed HTML element ID for the widget.}
}
\value{
An \code{htmlwidget} object.  See \code{\link{rd3survival}} for
  rendering and export details.
}
\description{
High-level wrapper around \code{\link{rd3survival}} that accepts a raw data
frame and column names, fits the Kaplan-Meier model internally, and
enriches the chart with:
\itemize{
  \item a \strong{k × 3 annotation table} (Cohort | Endpoint (unit) | P-value),
  \item dashed \strong{median crosshairs} per strata,
  \item a \strong{number-at-risk table} below the plot, and
  \item a \strong{log-rank p-value} for stratified comparisons.
}
All rendering is performed by the D3.js engine; R only prepares the data
and computes summary statistics.
}
\details{
\strong{Patient counts:} if \code{df} contains a column named
\code{upk_key}, unique values are counted; otherwise \code{nrow(df)} is
used.  This accommodates datasets where multiple rows per patient exist.

\strong{Log-rank p-value:} computed via
\code{\link[survival]{survdiff}} only when \code{strat_var} is supplied.
Formatted as \code{"< 0.001"} or three decimal places.

\strong{Medians:} extracted from \code{summary(survfit)$table}.  \code{NA}
medians (curve never reaches 50\%) are displayed as \code{"NR"}.

\strong{Number at risk:} evaluated at each x-break via
\code{summary(survfit, times = x_breaks, extend = TRUE)}.
}
\examples{
library(survival)
lung2 <- lung
lung2$status     <- lung2$status - 1
lung2$sex_label  <- ifelse(lung2$sex == 1, "Male", "Female")

# Single-arm
rd3survival_km(lung2, time_var = "time", event_var = "status",
               title = "Overall Survival", endpoint = "OS",
               time_unit = "Days", x_break_every = 100)

# Stratified with annotation table and risk table
rd3survival_km(lung2,
  time_var      = "time",
  event_var     = "status",
  strat_var     = "sex_label",
  color         = c("#e94560", "#0f3460"),
  title         = "Overall Survival by Sex",
  subtitle      = "NCCTG Lung Cancer Dataset",
  title_x       = 0.08,
  subtitle_x    = 0.08,
  endpoint      = "OS",
  time_unit     = "Days",
  x_break_every = 100,
  y_format      = ".0\%",
  bg_color      = "#1a1a2e"
)

}
\seealso{
\code{\link{rd3survival}} for the lower-level widget;
  \code{\link[survival]{survfit}}, \code{\link[survival]{survdiff}}.
}
